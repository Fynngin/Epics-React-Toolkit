export interface ApiImage {
    cardSide: string
    id: string
    name: string
    parentId: number
    parentType: string
    position?: string
    properties?: object
    treatmentId?: string
    url: string
}