export interface SpinnerItemProperties {
    cardTemplates: {id: number, quantity: number}[]
    coins: number
    craftingcoins: number
    pack_templates: {id: number, quantity: number}[]
    random_card_template: number
    silvercoins: number
}